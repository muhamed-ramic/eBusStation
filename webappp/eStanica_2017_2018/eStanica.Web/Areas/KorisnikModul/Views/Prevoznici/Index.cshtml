@model List<PrevozniciIndexViewModel>

<ul id="ulOfTravels">
   <p style="font-size:24px;color:white">PREVOZNICI</p>
    @foreach (var x in Model)
    {
        <li id="@x.Id">@x.Naziv</li>
    }
</ul>
